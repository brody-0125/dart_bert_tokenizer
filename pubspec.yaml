name: dart_bert_tokenizer
description: A pure Dart BERT WordPiece tokenizer with Hugging Face tokenizer.json loading, dynamic AddedToken support, Unicode offsets, and verified model fixtures.
version: 1.2.0
repository: https://github.com/brody-0125/dart_bert_tokenizer
topics:
  - nlp
  - bert
  - tokenizer
  - machine-learning
  - wordpiece

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  crypto: ^3.0.7
  lints: ^6.0.0
  test: ^1.25.6
